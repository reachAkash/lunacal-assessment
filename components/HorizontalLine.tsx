import React from "react";

const HorizontalLine = () => {
  return (
    <hr
      style={{
        boxShadow: "0 4px 8px rgba(0, 0, 0, 0.6)",
      }}
      className="h-1 w-[90%] m-auto bg-gray-800  border-0 rounded-full"
    />
  );
};

export default HorizontalLine;
